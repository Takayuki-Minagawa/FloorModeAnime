# 機能追加 調査・提案まとめ

> Floor Mode Anime（床構面の鉛直成分モード形 3Dアニメ表示・静的Webアプリ）への追加機能候補。
> 複数のサブエージェントによる多角的調査（**UX/UI** / **技術・データ処理** / **構造・振動工学ドメイン**）の結果を統合したもの。
> 既存機能（再生/停止、速度・倍率スライダー、表示切替、線色/太さ設定、テーマ/言語切替、PNG保存、JSON単一読込）は除外し、未実装の追加機能に絞っています。

調査日時点のコード（`refactor/code-quality` マージ後の `main`）を対象に調査しました。**実装は行っていません。**

---

## 0. 先に解消すべき既知の不整合 — 位相 φ0

3観点すべてで指摘された、**仕様と実装の乖離**です。

- **CLAUDE.md の式**: `u_i(t) = S · A_ref · (uz_i,m / Umax_m) · sin(2π f_m t + φ0)`
- **実装** (`animation.js`): `... · sin(2π f_m t)` — **`+ φ0` が欠落**。`parser.js` も φ0 を読み込まず、データ形式にもフィールドが無い。

通常の定常実モード（全節点同位相）では φ0=0 が妥当で見た目への影響は限定的ですが、仕様と実装が乖離しています。後述の **モード合成（進行波・うなり表現）** や **複素モード** 拡張の前提にもなるため、**最初に φ0 を実装へ追加し、データ形式に `phase0`（モード別ラジアン、省略時0・後方互換）を追加する**ことを推奨します。実装難易度: **低**。

---

## 1. 機能候補一覧（横断サマリ）

| # | 機能 | 観点 | 価値 | 難易度 | 追加データ | 優先度 |
|---|------|------|------|--------|-----------|--------|
| F1 | 位相 φ0 対応（仕様整合） | ドメイン | 仕様整合・他機能の基盤 | 低 | 任意(phase0) | **高** |
| F2 | 時間スクラブバー＋コマ送り | UX | 任意位相の静止表示・報告書作成 | 中 | なし | **高** |
| F3 | 再生/停止トグル＋キーボードショートカット | UX | 操作効率・標準的UX | 低 | なし | **高** |
| F4 | カメラ プリセットビュー（平面/正面/側面/等角）＋リセット | UX | 構造系必須の構図・PNG構図統一 | 中 | なし | **高** |
| F5 | 変位カラーマップ＋カラーバー凡例 | UX/ドメイン | 腹・節・振幅分布が一目で判る | 中〜高 | なし | **高** |
| F6 | 節点変位の数値表示（ツールチップ/テーブル） | ドメイン | モード照査・定量確認（現状最大の弱点） | 中 | なし | **高** |
| F7 | 単位系の明示＋スケールバー | ドメイン | 誇張表示と実変位の対応明示・誤読防止 | 低〜中 | なし(既存meta活用) | **高** |
| F8 | 居住性評価補助（歩行加振・共振チェック） | ドメイン | 床振動アプリのドメイン核心・一次照査 | 中 | なし | **高** |
| F9 | ドラッグ&ドロップ読込 | 技術 | 操作高速化・解析出力を直接投入 | 低 | なし | **高** |
| F10 | ユニットテスト基盤（Vitest）＋CI | 技術 | リグレッション防止・品質ゲート | 中 | なし | **高** |
| F11 | スライダー＋数値入力ボックス併用 | UX | 正確な値設定・図の再現性 | 低 | なし | 中 |
| F12 | 最大変位節点ハイライト＋数値 | UX | モード代表点の即時把握 | 低〜中 | なし | 中 |
| F13 | キャンバス内 情報オーバーレイ（タイトル/モード/f/倍率） | UX | PNGに文脈を焼き込み報告書に直接利用 | 中 | なし | 中 |
| F14 | 複数ケース/複数ファイル管理・切替 | 技術 | 解析ケース比較 | 中 | なし | 中 |
| F15 | バリデーション結果のダウンロード／該当要素ハイライト | 技術 | デバッグ実用性 | 中 | なし | 中 |
| F16 | 現フレーム変位データ出力（CSV/JSON） | 技術 | 数値検証・他ツール連携 | 低 | なし | 中 |
| F17 | アニメGIF/WebM録画 | UX/技術 | 動的モード形の共有・プレゼン | WebM中/GIF高 | なし | 中 |
| F18 | 複数モードの重ね合わせ（モード合成） | ドメイン | 実挙動（うなり・複雑振動）再現 | 中 | なし(φ0前提) | 中 |
| F19 | 面（床スラブ）サーフェス表示 | ドメイン | スラブのたわみモードを直感表示 | 中〜高 | あり(panels) | 中 |
| F20 | URL/クエリでの状態共有・設定の永続化 | 技術 | 「この見た目で共有」 | 中 | なし | 中 |
| F21 | CSV/他解析ソフト形式のインポート | 技術 | 実務データの取込負担軽減 | 中〜高 | なし | 中 |
| F22 | 規格化方法の選択（最大変位 ↔ 質量正規化） | ドメイン | FEM出力との整合 | 中 | あり(mass) | 中 |
| F23 | 減衰自由振動の包絡線表示 | ドメイン | 減衰挙動の教育的可視化 | 中 | あり(ζ) | 低 |
| F24 | 特定節点の時刻歴グラフ | ドメイン | 時間応答（位相・うなり・減衰）の定量把握 | 中 | なし | 低 |
| F25 | モード参加質量・有効質量比 表示／並替・フィルタ | ドメイン | 支配モードの定量評価 | 中 | あり(mass) | 低 |
| F26 | 複数モードの並列比較表示（画面分割/重畳） | UX | モード間の形状差比較 | 高 | なし | 低 |
| F27 | パネル折りたたみ＆モバイルタッチ最適化 | UX | 狭画面・現場利用の操作性 | 中 | なし | 低 |
| F28 | 大規模モデル向け描画最適化 | 技術 | 実モデル（数千節点）でのパフォーマンス | 中 | なし | 低 |
| F29 | PWA化・オフライン対応 | 技術 | 現場オフライン利用 | 中 | なし | 低 |

---

## 2. 観点別の詳細

### 2-1. UX/UI 観点

- **F2 時間スクラブバー＋コマ送り**: 1周期 `T=1/f` を範囲とするシークバーで任意時刻へジャンプ、`T/N` 刻みのコマ送り。最大変位の瞬間を静止表示でき、PNG保存と相性が良い。`animation.js` に **`setTime(t)` / `getPeriod()` の追加が必要**（現状 `_time` は `update()` 内でのみ加算され外部設定不可）。F17（録画）の前提基盤にもなる。
- **F3 トグル＋ショートカット**: `Space`=再生/停止、`←/→`=コマ送り、`R`=リセット等。入力フォーカス中（select/input）は無効化が必要。`play()/stop()/isPlaying()` の既存IFで足りる。
- **F4 カメラプリセット**: 平面図・立面図確認は構造系で必須。`viewer.js` に `setView(preset)` を追加（既存のカメラ配置ロジック・`_lFloor`・中心座標を再利用、座標変換は `geometry.toThree` 準拠）。
- **F5 変位カラーマップ**: 青↔白↔赤の diverging カラーで `uz/Umax`（または瞬時変位）を表現。LineMaterial を `vertexColors` 対応に変更し `updateDeformed` で色属性も更新。本アプリの主目的（モード形把握）に直結し情報量が大きい。LineSegments2 の頂点カラー設定がやや煩雑な点が難所。
- **F11 数値入力併用 / F12 最大変位ハイライト / F13 情報オーバーレイ**: いずれも低〜中コストの実用改善。F13 をPNGに焼き込む場合はCSS2Dオーバーレイではなく **canvas合成** が必要（現状ラベルはPNGに含まれない）。
- **F26 並列比較 / F27 モバイル最適化**: F26 は `AnimationController` が単一モード前提（`_currentMode`）のためアーキテクチャ影響大で低優先。

### 2-2. 技術・データ処理 観点

- **F10 テスト基盤（Vitest）＋CI（最重要の土台）**: `parser/validator/animation/geometry` は副作用のない純ロジックでテスト容易（`u_i(t)` 公式、`Umax_m` 正規化、`E_*` エラーコード、camelCase変換、`L_floor=0` ガード等）。Viteプロジェクトのため Vitest 導入摩擦は最小。`fix:` 系コミット（Issue#4/#5等）の再発防止になる。`.github/` は既存のため CI 追加も容易。
- **F9 ドラッグ&ドロップ**: 既存 `handleFileLoad(jsonText)` をそのまま再利用。`dragover`/`drop` リスナー＋FileReaderのみ。
- **F14 複数ケース管理**: 1ケース=1 `{floorData, animController}` を Map 保持して切替えるだけで、公開IF無改修で実現可能。`app.js` の単一 `animController` を Map 化。
- **F15 検証結果出力/ハイライト**: 検証結果は既に `{code,message}` 構造。`validator.js` に `nodeId`/`lineIndex` 等の **機械可読フィールドを後方互換で併設** すると将来のハイライト/出力が容易。
- **F16 変位データ出力**: `getDisplacedZ(nodeId)` が既に公開IFにあり、節点列挙＋呼び出しで実装可能。PNGと同じ「停止中のみ」ガードを流用。
- **F17 録画**: WebM は `MediaRecorder`+`canvas.captureStream()` でブラウザネイティブ・追加依存ゼロ（`preserveDrawingBuffer:true` が既に有効）。GIFはライブラリ要で高コスト。
- **F20 状態共有 / F21 CSVインポート / F28 最適化 / F29 PWA**: いずれも静的ホスティング適合。F28 は `getDisplacedZ` の `sin` 共通項キャッシュ（`sin(2πft)` を1回計算して係数を掛ける形）が低コスト・効果明確で先行価値あり。F29 は `base` パス整合に注意。

> **設計上の所見**: 既存のモジュール境界（parser戻り値の正規化構造、`AnimationController`/`FloorViewer` の公開IF）が安定しており、**F14/F16/F21 は本体IF無改修で増築できる余地が大きい**。

### 2-3. 構造・振動工学ドメイン観点

- **F1 φ0**（第0章参照）。
- **F6 節点変位の数値表示**: `uz`（正規化）・実変位 `u_i(t)`・`z_i'(t)` をホバー/テーブルで表示。腹・節（節線）位置と相対振幅を定量確認でき、**可視化だけで数値が読めない現状の最大の弱点を補う**。Raycaster ピッキング or 独立テーブルパネル。
- **F7 単位とスケールバー**: 変形は誇張表示（`A_ref=L/10`）のため、実変位との対応を明示しないと誤読を招く。`meta.length_unit`（既存・未活用）と周期 `T=1/f` を併記。
- **F8 居住性評価補助**: モード振動数 f を歩行加振帯（1.5–2.5Hz と倍音）と比較し共振リスクを色/警告で提示。AIJ居住性評価指針・ISO 10137・SCI P354 等の一次スクリーニングに直結。既存データのみで実現可能。
- **F18 モード合成**: `u_i(t)=Σ_m S·A_ref·(uz_i,m/Umax_m)·sin(2π f_m t + φ0_m)`。異なる f によるうなり・複雑振動を再現。φ0（F1）が前提。
- **F19 面サーフェス表示**: 床は本来「面」。`panels`/`faces`（節点ID列）をデータ追加し Mesh 描画。F5 コンターとの相乗効果大。三角形分割が必要。
- **F22 規格化選択 / F25 参加質量**: FEM出力は質量正規化が標準。節点質量データが前提。鉛直成分のみのため水平方向の参加質量は算出不可（制約）。
- **F23 減衰 / F24 時刻歴グラフ**: 自由振動の減衰挙動・時間応答の可視化。教育的価値。

---

## 3. 推奨ロードマップ

依存関係と費用対効果を踏まえた着手順の推奨です。

1. **土台を固める**: `F10`（テスト基盤＋CI）→ 以降の変更を安全化。`F1`（φ0 整合）で仕様乖離を解消。
2. **低コスト・高頻度の実用改善**: `F9`（D&D）、`F3`（ショートカット/トグル）、`F11`（数値入力）、`F16`（変位データ出力）。既存IFをそのまま再利用。
3. **基盤系の機能**: `F2`（スクラブバー = `animation.js` に `setTime`/`getPeriod` 追加）→ `F17`（録画）の前提。`F4`（カメラプリセット）、`F12`（最大変位ハイライト）。
4. **ドメイン価値の核心**: `F6`（数値表示）、`F7`（単位/スケールバー）、`F8`（居住性チェック）。床振動アプリとしての実務価値を最も底上げ。
5. **視覚的訴求**: `F5`（カラーマップ）→ `F19`（面表示）→ `F18`（モード合成）。描画層改修が重いので基盤整備後。
6. **用途に応じて**: `F14`（複数ケース）、`F15`（検証出力）、`F20`（状態共有）、`F21`（CSV）、`F22/F25`（質量系）、`F23/F24`（減衰/時刻歴）、`F26/F27/F28/F29`。

---

## 付録: 主要な実装影響箇所

| 機能群 | 主な変更ファイル | 公開IFへの影響 |
|--------|------------------|----------------|
| F1, F18, F22, F23 | `animation.js`, `parser.js`, `validator.js`, データ形式 | `AnimationController` 拡張（`setTime`/`setPhase`/`setNormalization` 等） |
| F2, F3, F11, F12 | `ui.js`, `index.html`, `i18n.js`, `styles.css` | `animation.js` に `setTime`/`getPeriod` 追加が必要 |
| F4, F5, F13, F19 | `viewer.js`, `index.html`, `styles.css` | `FloorViewer` 拡張（`setView`/カラーマップ/サーフェス） |
| F6, F24 | `viewer.js`(ピッキング) or 新規パネル, `ui.js` | 既存 `getDisplacedZ` で算出可 |
| F9, F14, F15, F16, F20, F21 | `app.js`, `ui.js`, 新規 `importers/` 等 | 多くは本体IF無改修で増築可 |
| F10 | `package.json`, 新規 `tests/`, `.github/workflows/` | 本体コード変更不要 |

> CLAUDE.md の「モジュール間インターフェース」表は、`AnimationController` に `setTime`/`getPeriod`/`setPhase` 等を追加する際に更新が必要です。
